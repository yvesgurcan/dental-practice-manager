export default (viewport) => {
  return ({
    timer: {
      height: '50%',
      width: 2,
      background: 'gray',
      textAlign: 'center',
      margin: 'auto',
      transformOrigin: 'bottom center',
    },
  })
}